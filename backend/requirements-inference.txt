# TrackCast Inference-Only Dependencies
# This file contains only the dependencies needed for the inference service
# Excludes training, visualization, and development dependencies

# Web framework
fastapi>=0.100.0
uvicorn>=0.23.0
gunicorn>=21.2.0

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
alembic>=1.11.0

# Core data processing (inference only)
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0  # Only for StandardScaler and preprocessing

# Machine Learning (inference only)
torch>=2.0.0

# API client
httpx>=0.24.0
pydantic>=2.0.0
requests

# Configuration
pyyaml>=6.0.0
python-dotenv>=1.0.0
pytz>=2023.3

# Task scheduling
schedule>=1.2.0
apscheduler>=3.10.0

# Production dependencies
# NOTE: Training dependencies excluded:
# - matplotlib, seaborn (visualization)
# - shap (model explanation) 
# - xgboost (if not needed for inference)
# - pytest and testing tools (moved to dev requirements)
# - code quality tools (black, flake8, etc.)